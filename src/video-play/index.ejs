<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="cache-control" content="max-age=1800">
    <title></title>
    <script src="js/sdk.js?v=<%=new Date().getTime()%>"></script>
    <script src="js/fit.js?v=<%=new Date().getTime()%>"></script>
    <link rel="stylesheet" href="css/index.css?v=<%=new Date().getTime()%>">
</head>
<body>
<div class="container" id="video-play">
    <video src=""  id='video1'></video>
    <div class="btn-play" data-syncactions="btn-play" style="display: none">
        <img src="image/pause.png" alt="">
    </div>
    <div class="end-tip">That's the end!</div>
    <div id="tg">
        <img src="image/tg.png" alt="" class="tg" style="display: none">
        <div class="tg-content">
            <ul></ul> 
        </div>
    </div>
</div>
<script type="text/javascript" src="content.js?v=<%=new Date().getTime()%>"></script>
<script type="text/javascript" src="js/index.js?v=<%=new Date().getTime()%>"></script>
<script type="text/javascript">
    window.onload = function () {
        var sync = parent.window.syncActions && parent.window.syncActions.isSync;
        console.log(sync)
        if(!sync) {
            return;
        }
        var currentPage = parent.window.communicationWithAc.currentPage;
        if(configData.page != currentPage) {
            if(configData.page === 1){
                $('#currentiframe', window.parent.document)[0].src = parent.window.communicationWithAc.getPageUrl(currentPage);
            }else{
                return false;
            }
            
        }
        parent.window.syncActions.isPracticePage(false);
        parent.window.syncActions.loadingIframe(true);
    }
</script>
</body>
</html>
